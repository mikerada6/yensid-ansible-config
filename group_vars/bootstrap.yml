# inventory/group_vars/bootstrap.yml
bootstrap_user:   ansible
bootstrap_group:  ansible
bootstrap_shell:  /bin/bash
bootstrap_public_key: "{{ lookup('env','HOME') + '/.ssh/fantasyland_dev_ansible.pub' }}"
